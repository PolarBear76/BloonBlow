<center>

<%= image_tag(current_user.avatar.url(:medium), class: "profimg") %> 
	<h1 class="darkpurpleFont"><%= current_user.name %></h1>

</center>
<hr>

<div id="pins" class="transitions-enabled">
  <% current_user.pins.each do |pin| %>
    <div class="pin-index box panel panel-default">
        
        <% if current_user && pin.user == current_user %>
        <p class="currentUserName desc"><strong><%= pin.user.name if pin.user %></strong></p>  
        <% else %>
        <p class="desc"><strong><%= pin.user.name if pin.user %></strong></p>
        <% end %>

      <div class="blur">
        <%= link_to image_tag(pin.image.url(:medium)), pin %>
        </div>
      <div class="pinDesc panel-body accentColor">
        
        <p>
        <%= pin.description %>
        </p>
      </div>
    </div>
  <% end %>
 
 </div> 